{
  "id": "cold-start-gate",
  "description": "Pre-tag release gate: executes the documented new-user journey (install, quickstart, handoff draft/lint, doctors, gitignore guards) in a throwaway sandbox. Run from the repo root: brigade runbook run docs/runbooks/cold-start-gate.json",
  "approved": true,
  "allowed_commands": ["bash"],
  "steps": [
    {
      "id": "stranger-journey",
      "run": "bash docs/runbooks/cold-start-gate.sh",
      "timeout_seconds": 600
    }
  ]
}

---
title: Content Guard Pre-Push
tags: [security, hooks, git]
description: Pre-push hook blocks personal data and live credentials from leaving the repo.
---

# Content Guard Pre-Push

Never bypass with `--no-verify`. Fix the leak or add a reviewed inline allow-tag on the offending line. CI content-guard fails on hostnames, account IDs, and live auth profiles in fixtures and templates.
